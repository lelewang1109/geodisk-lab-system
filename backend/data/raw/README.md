# Raw data boundary

Raw NetCDF files are intentionally not copied into this repository. `config/datasets.yaml` points, by a project-relative path, to `../../data/2000`. E0 audits that directory before downstream loading. No source NetCDF is modified.

